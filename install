#!/bin/bash

DOTFILES=$(cd $(dirname "${BASH_SOURCE[0]}") && pwd)

source "$DOTFILES/src/functions.sh"

install_packages
install_docker
install_astronvim
install_php
install_warpterminal
install_tmux
install_zapzsh

